import type { Meta } from '@storybook/react';

import { Todo } from './Todo';

const meta: Meta<typeof Todo> = {
  title: 'Example/Todo',
  component: Todo,
  tags: ['autodocs'],
};

export default meta;
